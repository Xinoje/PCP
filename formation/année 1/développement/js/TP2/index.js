const body = document.querySelector('body');
// Crée un paragraphe
const para = document.createElement('p');
// Ajoute du texte au paragraphe
para.textContent = 'JS c\'est super';
// Insère le paragraphe à la fin du body
body.append(para);
// Supprime le titre
const titre = document.querySelector('h1');
titre.remove()

const = 


const ecoute_event = document.getElementById('para')
button.addEventListener('click', function() {
    return textContent('!')
}